# ClipRaft

ClipRaft 是一个本机优先的 Windows 剪贴板工作区，在不打断当前工作的前提下暂存并重新使用复制内容。

## Language

**边缘触发条（Edge Handle）**:
浮窗面板收起后留在桌面左侧或右侧边缘的窄条，是用户主动展开面板的入口。
_Avoid_: 悬浮球、任务栏

**复制预览（Copy Peek）**:
捕获复制动作后短暂出现的窄幅反馈区域；它展示新内容，但不主动取得键盘焦点。
_Avoid_: 完整面板自动弹出、复制通知

**浮窗面板（Panel）**:
从边缘触发条展开的主要交互区域，用于浏览和操作已经捕获的内容。
_Avoid_: 主窗口、剪贴板窗口

**剪贴卡片（Clip Card）**:
一次复制动作在历史流中产生的一个可见内容单元；不同复制动作始终是不同卡片。
_Avoid_: 剪贴项、历史行

**模态（Modality）**:
剪贴卡片所属的内容类别，首版包括文本、图片和文件；模态决定卡片的视觉语言与可用操作。
_Avoid_: MIME 类型、文件格式

**原始表示（Representation）**:
一次复制中属于同一剪贴卡片的具体内容表示，例如纯文本、HTML、RTF、位图或文件列表；卡片可保留多个原始表示，但只有一个主要模态。
_Avoid_: 独立卡片、模态

**历史流（History Stream）**:
按最近使用时间排列的剪贴卡片集合，也是产品所称的多存储位；重复内容复用已有卡片并移动到顶部。
_Avoid_: Board、收藏夹、固定槽位

**固定卡片（Pinned Card）**:
用户明确标记为长期保留的剪贴卡片；它不参与重复内容导致的重排，也不被自动清理。
_Avoid_: Board、永久剪贴板

**保留副本（Keep Copy）**:
用户主动要求应用保存文件实体的动作；未执行时，文件卡片只引用来源路径。
_Avoid_: 自动备份、文件同步

**自动粘贴（Auto Paste）**:
用户确认使用某张卡片后，将其原始表示恢复到系统剪贴板并粘贴到此前的活动应用。
_Avoid_: 单击即粘贴、后台粘贴

**历史保留（History Retention）**:
用户可控制的跨应用重启保留能力；关闭后只保留当前会话并在彻底退出时清空，切换开关本身不会删除既有历史，除非用户另行确认。
_Avoid_: 云同步、备份
